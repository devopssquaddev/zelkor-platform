# Parked: no compose, pytest, Langfuse evals, or checkout.
# Do not add pull_request / push until the eval harness exists.
name: AI Red Team & Guardrail Evals

on:
  workflow_dispatch:

jobs:
  adversarial-eval:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Parked
        run: echo "Adversarial eval is parked"
