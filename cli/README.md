# zelkor CLI

```bash
pip install -e ./cli
zelkor --help
```

One packager for LangGraph / Aegra and Deep Agents. Named envs are kubecontext + namespace only — no hosts, tokens, or DSNs in the env file.

Requires a live Zelkor platform (`./install.sh` or an existing cluster). Does not install the platform.
