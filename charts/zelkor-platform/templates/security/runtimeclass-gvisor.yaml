{{- if .Values.security.sandbox.enabled }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: {{ .Values.security.sandbox.runtimeClass | default "gvisor" }}
  labels:
    {{- include "zelkor-platform.labels" . | nindent 4 }}
handler: runsc
{{- end }}
