{{- $mode := include "zelkor-platform.dbMode" . -}}
{{- if and .Values.clickhouse.enabled (or (eq $mode "in-cluster-basic") (eq $mode "operator-cr")) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "zelkor-platform.fullname" . }}-clickhouse
  labels:
    {{- include "zelkor-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: clickhouse
type: Opaque
stringData:
  password: {{ required "clickhouse.auth.password is required when clickhouse.enabled" .Values.clickhouse.auth.password | quote }}
{{- end }}
