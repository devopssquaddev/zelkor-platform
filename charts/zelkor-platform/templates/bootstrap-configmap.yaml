{{/*
Zelkor Platform — placeholder chart templates.
Subcharts and component templates will be added in Phase 1 implementation.
*/}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-bootstrap
  labels:
    app.kubernetes.io/name: zelkor-platform
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  message: "Zelkor Platform Base Tier — chart scaffold. Component templates coming soon."
