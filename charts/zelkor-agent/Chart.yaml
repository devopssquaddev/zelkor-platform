apiVersion: v2
name: zelkor-agent
description: ClusterIP worker for one or more graphs in one image. Self-registers via sharedRoute HTTPRoute when host + gateway are set. CLI workers use zelkor-aegra-deep.
type: application
version: 2.0.0
appVersion: "2.0.0"
