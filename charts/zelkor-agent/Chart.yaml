apiVersion: v2
name: zelkor-agent
description: ClusterIP worker for one independently released graph (FROM zelkor-aegra). Register via sharedRoute HTTPRoute or platform aegra.workers.
type: application
version: 0.1.0
appVersion: "0.1.0"
